# Copy to .env and fill in values. Never commit .env.

# Supabase PostgreSQL
# 1. In Supabase: Project Settings → Database
# 2. Under "Connection string" choose "URI" (direct) or "Transaction" (pooler)
# 3. Copy the URI and paste below, then replace [YOUR-PASSWORD] with your database password
#    (same password you set when creating the project, or reset it on that page)
DATABASE_URL="postgresql://postgres.jmfshcpgtuqdjmtpexqg:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"
